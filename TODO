- Cache backend responses
- Backend authentication
    - Requires backend abstraction
- Backend abstraction: data Backend, HashMap BackendID Backend, Route -> BackendID
- File backend
- Static files
- Backend response types
    - YAML
    - Plain text
    - HTML (parse as DOM, expose DOM-like API to Ginger)
    - XML?
    - Markdown / RST / Textile / ... (Pandoc)
